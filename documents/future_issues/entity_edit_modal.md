# Entity Edit Modal

## Objective

Replace the inline sidebar editing with a dedicated modal for editing entity details. The sidebar becomes a read-only detail view showing entity fields, parent link, and relationships. Clicking an "Edit" button opens a modal with all editable fields (name, display name, description, parent). This separates read and write concerns, declutters the sidebar, and allows the modal to support richer editing features in the future.
