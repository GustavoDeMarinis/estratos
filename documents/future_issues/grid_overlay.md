# Grid Overlay for Territorial Representation

## Objective

Implement a discrete grid overlay on the map for painting territories, rivers, forests, and other spatial extents. Users paint grid cells (squares or hexagons) to define areas or trace paths. Each painted region is stored as an array of cell coordinates linked to an entity. This replaces any need for freeform polygon geometry.
