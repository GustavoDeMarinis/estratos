# Map Scale and Distance Measurement

## Objective

Allow maps to define a scale (`km_per_pixel`) that gives physical meaning to distances. Users can set the scale by entering a value directly or by calibrating with two reference points and a known distance. When a scale is defined, a measurement tool lets users click two points on the map to see the straight-line distance in km (or m if under 1 km).
