# New Entity Types

## Objective

Expand the entity type registry with environment and cultural entity types. Candidates include River, Mountain, Forest, Faction, and a generic Entity catch-all. Each new type follows the established pattern: dedicated table, registry entry in `EntityTypes`, layer assignment, and structural parent relationships where applicable. The registry architecture from Issue 8 makes adding new types a single-entry change.
