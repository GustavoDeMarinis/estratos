# Entity Search

## Objective

Implement a search bar that lets users find entities by name across all types. Matching entities are highlighted on the map (or non-matching pins are dimmed). Entities without map placement appear in search results and can be navigated to via their detail view. Search is the primary navigation tool for large worlds.
